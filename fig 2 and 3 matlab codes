N = 2500;
p = 0.3;
seq = rand(1,N) < p;
freq = [sum(seq==0) sum(seq==1)];
figure;
subplot(2,1,1);
stem(seq(1:50),'filled');
title('First 50 Bits of Bernoulli Sequence');
subplot(2,1,2);
bar(freq);
title('Frequency of 0s and 1s');
